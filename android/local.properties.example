# Rename this file to "local.properties" and update the SDK path below.
# Do NOT commit local.properties — it contains machine-specific paths.

sdk.dir=C\:\\Users\\<you>\\AppData\\Local\\Android\\Sdk
# sdk.dir=/Users/<you>/Library/Android/sdk

# Optional: override Gradle JDK (must be 17+). If unset, see gradle.properties.
# jdk.dir=C\:\\Program Files\\Android\\Android Studio\\jbr

# Optional FitSense API (cloud auth / sync / merchant catalogue).
# fitsense.api.baseUrl=https://fitsense-api-1rne.onrender.com

# Optional merchant catalogue defaults (also configurable in Settings).
# Requires device cloud auth + org membership — no permanent API keys on-device.
# fitsense.merchant.orgId=org_...
